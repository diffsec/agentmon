# agentmon Docker Compose Configuration
#
# Usage:
#   docker-compose up -d          # Start agentmon
#   docker-compose logs -f        # View logs
#   docker-compose down           # Stop agentmon
#
# Access:
#   HTTP API: http://localhost:18080
#   gRPC API: localhost:9090

version: '3.8'

services:
  agentmon:
    image: ghcr.io/diffsec/agentmon:latest
    # Or build locally:
    # build:
    #   context: .
    #   dockerfile: Dockerfile.example
    
    container_name: agentmon
    hostname: agentmon
    
    # Required capabilities for full functionality
    cap_add:
      - SYS_ADMIN      # Required for FUSE, namespaces
      - NET_ADMIN      # Required for network namespace, iptables
    
    # Required devices
    devices:
      - /dev/fuse      # Required for FUSE filesystem
    
    # Security options
    security_opt:
      - apparmor=unconfined    # Required for namespace operations
      # For SELinux systems, also add:
      # - label=disable
    
    # Port mappings
    ports:
      - "18080:18080"    # HTTP API
      - "9090:9090"    # gRPC API
    
    # Volume mounts
    volumes:
      # Workspace directory - where agent code runs
      - ./workspaces:/workspaces
      
      # Custom configuration (optional)
      - ./config:/etc/agentmon:ro
      
      # Persistent data (session history, logs)
      - agentmon-data:/var/lib/agentmon
      
      # Logs
      - agentmon-logs:/var/log/agentmon
    
    # Environment variables
    environment:
      - AGENTMON_LOG_LEVEL=info
      # Policy selection (name without .yml suffix). Must be in policies.allowed or defaults to policies.default.
      # - AGENTMON_POLICY_NAME=default
      # Optional env iteration block via shim for commands with env_policy.block_iteration=true
      # - AGENTMON_ENV_BLOCK_ITERATION=0
      # - AGENTMON_HTTP_ADDR=0.0.0.0:18080
      # - AGENTMON_GRPC_ADDR=0.0.0.0:9090
    
    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 512M
    
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:18080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    
    # Restart policy
    restart: unless-stopped
    
    # Logging
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "3"

# Named volumes for persistence
volumes:
  agentmon-data:
    driver: local
  agentmon-logs:
    driver: local

# Optional: custom network
networks:
  default:
    name: agentmon-network
